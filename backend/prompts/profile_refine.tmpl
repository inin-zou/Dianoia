You are refining an existing police composite sketch portrait. The goal is to apply a specific modification while maintaining 95% consistency with the existing face.

## Current Composite Description

{{.CurrentImageDescription}}

## Refinement Instruction

{{.Instruction}}

## Refinement Rules

- PRESERVE all existing facial features that are NOT mentioned in the refinement instruction. The face must remain recognizable as the same person.
- APPLY only the specific change requested. Do not alter unrelated features.
- Maintain the same:
  - Head angle and pose (front-facing)
  - Lighting (even, studio-style)
  - Background (plain, neutral gray)
  - Image framing (head and shoulders)
  - Overall facial structure and proportions (unless the instruction specifically modifies them)
  - Skin tone (unless the instruction specifically modifies it)
  - Eye color and shape (unless the instruction specifically modifies them)
- The modification should look natural and integrated, not pasted on or artificial.
- If the instruction is ambiguous (e.g., "add a beard" without specifying type), choose a moderate, natural-looking default (e.g., short trimmed beard).
- If the instruction contradicts a previous feature (e.g., "remove glasses" when glasses are present), cleanly remove it and show the natural face underneath.

Generate the updated portrait with the requested modification applied. The result should look like the same person with only the specified change.
